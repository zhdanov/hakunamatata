# helm/init-container-wait-http-200.yaml
#
image: {{ env "appname" }}-wait-http-200
from: alpine:3.13.0
ansible:
  beforeInstall:
  - name: install packages
    shell: |
      apk add --no-cache curl
